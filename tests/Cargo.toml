[package]
name = "tests"
version = "0.1.0"
authors = ["Benjamin Eder <barny.eder@gmail.com>"]
edition = "2018"
description = "Library crate containing tests for the syntax-tree crate"

[dependencies]
syntax-tree = {path = "../"}
shared = {path = "../shared"}
